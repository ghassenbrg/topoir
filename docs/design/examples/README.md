# Target document examples

These examples specify the proposed `topoir.dev/v1alpha2` / `DiagramWorkspace` contract. **They are not supported by the current v1alpha1 compiler.** They are fictional authored scenarios, not claims about a real deployed system.

| Example | Demonstrates |
| --- | --- |
| [Architecture](architecture.topoir.yaml) | Shared entity identity, required content, primary path, fixed medium and presentation constraints |
| [Process](process.topoir.yaml) | Lane ownership, decision outcomes, exception-independent branches and process-specific semantics |
| [Interaction](interaction.topoir.yaml) | Ordered messages, replies, activation and alternative event fragments |

During T10/T21/T23, turn these into target-schema validation fixtures. During T19/T22/T24, compile and visually inspect them. Keep examples synchronized with the owning contracts; do not silently modify them to work around an implementation gap. Syntax validation during design creation does not imply runtime compatibility.
