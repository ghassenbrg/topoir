# @topoir/cli

Agent-native `topoir` command-line interface.

```bash
topoir validate architecture.topoir.yaml
topoir render architecture.topoir.yaml --view overview --output architecture.svg
topoir doctor
```

See the [CLI reference](../../docs/cli.md).
