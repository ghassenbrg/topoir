{
  "name": "@topoir/renderer-svg",
  "version": "0.1.0-alpha.0",
  "description": "Deterministic scene graph, SVG renderer, and PNG exporter for TopoIR",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "publishConfig": { "access": "public" },
  "sideEffects": false,
  "files": ["dist"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap --clean",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@topoir/assets": "workspace:*",
    "@resvg/resvg-js": "^2.6.2",
    "@topoir/core": "workspace:*",
    "@topoir/schema": "workspace:*",
    "dejavu-fonts-ttf": "^2.37.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsup": "^8.5.0",
    "typescript": "^6.0.0"
  }
}
