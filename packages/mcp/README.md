# @topoir/mcp

Local stateless MCP 2026-07 stdio server for TopoIR. It exposes document validation, rendering, compiler inspection, icon search, and schema/example/diagnostics resources.

```bash
topoir-mcp
```

See the [MCP guide](../../docs/mcp.md).
