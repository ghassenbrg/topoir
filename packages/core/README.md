# @topoir/core

Semantic normalization, view projection, typed compiler intermediate representations, deterministic measurement/themes, and geometry quality gates for TopoIR.

This package deliberately does not choose a layout backend or renderer. See the [compiler architecture](../../docs/architecture.md).
