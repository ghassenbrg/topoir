# @topoir/layout-elk

Eclipse ELK compound-layout adapter implementing TopoIR's replaceable `LayoutEngine` contract. It supports nested groups, hierarchy edges, explicit ports, labels, orthogonal routes, and deterministic layout settings.

Raw ELK options are not a TopoIR authoring interface.
