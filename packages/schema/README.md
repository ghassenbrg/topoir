# @topoir/schema

Versioned semantic document types, JSON Schema, YAML/JSON parser, source maps, and structural diagnostics for TopoIR.

```ts
import { parseDocument, validateStructure } from "@topoir/schema";
```

See the [repository schema guide](../../docs/schema.md).
